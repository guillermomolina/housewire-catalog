kind: cable_type
id: Cable
icon: fa-minus
label: Cable sheath / multicore jacket
description_es: >
  Funda o mazo que agrupa conductores (contains). No conecta terminales;
  vive bajo cables:, no en elements.
defaults:
  color: BK
subtypes:
  power:
    label: Power (AC)
    defaults:
      color: BK
  earth:
    label: Protective earth bundle
    defaults:
      color: GNYE
  dc:
    label: DC
    defaults:
      color: BK
  signal:
    label: Signal / data
    defaults:
      color: GY
